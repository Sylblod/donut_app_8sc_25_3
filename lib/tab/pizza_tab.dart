import 'package:flutter/material.dart';

class Pizzatab extends StatelessWidget {
  const Pizzatab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pizzatab Tab Content'),
    );
  }
}
