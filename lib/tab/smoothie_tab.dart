import 'package:flutter/material.dart';

class Smoothietab extends StatelessWidget {
  const Smoothietab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Smoothietab Tab Content'),
    );
  }
}
